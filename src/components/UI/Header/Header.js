import React from 'react';
import classes from './Header.css';

const appHeader = (props) => (
    <div className="Header">
        <h2>REACT + ANT + FIREBASE</h2>
    </div>
);

export default appHeader;
